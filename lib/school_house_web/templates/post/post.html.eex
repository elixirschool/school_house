<section class="container w-3/4 mb-6 items-center prose">
  <div class="pt-6 pb-6">
    <span class="text-3xl font-bold"><%= @post.title %></span>
  </div>

  <div class="bg-light-gray rounded-xl p-4"><%= raw @post.excerpt %></div>

  <%= raw @post.body %>
</section>
